using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bullet : MonoBehaviour
{
    //float bulletSpeed = 600f;
    //Rigidbody rb;
    private void Start()
    {

    }
    void Update()
    {
        //Vector3 bulletDirection = new Vector3(0, 0, bulletSpeed);
        //rb.velocity = transform.TransformDirection(bulletDirection) * bulletSpeed;
        //rb.velocity = new Vector3(0, 0, bulletSpeed);
    }
}
